<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TM : Home</title>
    <link rel="stylesheet" href="/home.css">
</head>
<body>
    <%-include("./partials/header.ejs",{username : username,token : token,profile_picture: profile_picture})%>
    
    <div id="channel-container">
        <%if(channel.length>0){%>
            <%for(var i=0;i<channel.length;i++){%>
            <div class="channel" style="border: 1px solid black" id="<%=channel[i]._id%>">
                <h3>Room Name : <%=channel[i].roomname%></h3>
                <h5>Description : <%=channel[i].description%></h5>
                <p >Tags : <%=channel[i].tags%></p>
                <p class="timer">Created By : <%=channel[i].createdBy%></p>
            </div>
            <%}%>
        <%}else {%>
            <h3>You haven't joined/created any channels ye1!!</h3>
        <%}%>    
    </div>

    
    <script src="/home.js"></script>
</body>
</html>